# Fonts

Run `scripts/vendor-assets.ps1` or `scripts/vendor-assets.sh` to download the OpenMapTiles glyph PBF directories required by the bundled styles. The vendoring script intentionally excludes unused font families and upstream ZIP archives.
